% R setup Samatha

# Test post

Here is a summary of the `cars` dataset:

```{r}
summary(cars)
```

And here is a graph:

```{r fig.width=7, fig.height=6}
plot(cars)
```

```{r gCars}
library(ggplot2)
ggplot(cars, aes(x=speed, y=dist)) + geom_points()
```

Adding some text to test something.
